<Window x:Class="Andreeva_TZv2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Andreeva_TZv2"
        mc:Ignorable="d"
        Background="#FF212121"
        Title="DayAndNight" ResizeMode="NoResize" Height="380" Width="397">
    <Window.Resources>

        <SolidColorBrush x:Key="TextBox.Static.Border" Color="#FF1D1D1D"/>
        <SolidColorBrush x:Key="TextBox.MouseOver.Border" Color="Black"/>
        <Style x:Key="PasswordBoxStyle" TargetType="{x:Type PasswordBox}">
            <Setter Property="PasswordChar" Value="●"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
            <Setter Property="BorderBrush" Value="{StaticResource TextBox.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="KeyboardNavigation.TabNavigation" Value="None"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Bottom"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="AllowDrop" Value="true"/>
            <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type PasswordBox}">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0,0,0,3" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.Focus.Border}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsInactiveSelectionHighlightEnabled" Value="true"/>
                        <Condition Property="IsSelectionActive" Value="false"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="SelectionBrush" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Border  Background = "#FF545454" BorderBrush="Silver" Height="344" VerticalAlignment="Center" HorizontalAlignment="Center" Width="377" >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="75*"/>
                    <RowDefinition Height="71*"/>
                    <RowDefinition Height="74*"/>
                    <RowDefinition Height="46*"/>
                </Grid.RowDefinitions>
                <TextBox Style="{DynamicResource TextBoxK}" RenderTransformOrigin="0.5,0.5" x:Name="Login" Margin="75,8,76,10" Text="Введите логин" GotFocus="Login_GotFocus" LostKeyboardFocus="Login_LostFocus"/>
                <PasswordBox Style="{DynamicResource PasswordBoxStyle}" RenderTransformOrigin="0.5,0.5" x:Name="Password" Grid.Row="1" Margin="75,6,76,10"/>


                <Button BorderThickness="0" Background="#FF1D1D1D" Foreground="White" Content="Войти" FontSize="18" FontFamily="Exo2" FontWeight="Bold" Click="Button_Click" Grid.Row="2" Margin="126,30,127,31"/>
                <Button BorderThickness="0" Background="#FF1D1D1D" Foreground="White" Content="Выйти из системы" FontFamily="Exo2" Click="Button_Click_1" FontSize="14" Grid.Row="3" Margin="94,1,95,10"/>
            </Grid>
        </Border>
    </Grid>
</Window>